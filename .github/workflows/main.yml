
name: CI/build+test


on:

  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:


jobs:

  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a set of commands using the runners shell
      - name: Compile
        run: |
          cd calculator
          g++ -o out -std=c++14 main.cpp
      - name : Basic Test
        run: |
          cd calculator
          g++ -o -out -std=c++14 main.cpp
          echo "1+1" | ./out
          echo "(1+1)" | ./out
          echo "1*1" | ./out
          echo "3/2" | ./out
          echo "1-2" | ./out
      - name : Advanced Test
        run: |
          cd calculator
          g++ -o out -std=c++14 main.cpp
          echo "((1+1))" | ./out
          echo "(1+1)*2" | ./out
          echo "(1+1)^2" | ./out
          echo "((2-1)*2+1)^2" | ./out
          echo "(((2-1)*2+1)^2-1)/2" | ./out
          echo "-1+1" | ./out
          echo "1+(-1)" | ./out
          echo "(((-2-1)*2+1)^2-1)/2" | ./out
          echo "0.1-1" | ./out
          echo "0.1-0.1" | ./out
          echo "0.1^0.1" | ./out
          echo "sqrt9" | ./out
          echo "sqrt(8+1)" | ./out
